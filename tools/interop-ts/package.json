{
  "name": "pascal-mcp-sdk-interop-ts",
  "private": true,
  "description": "Cross-implementation checks: the official MCP TypeScript clients — stable v2 (2026-07-28) and v1 (the legacy era Claude Code speaks) — against pascal-mcp-sdk's mcpdemo over stdio and Streamable HTTP. Mirrors duetto's tools/crosscheck.py pattern.",
  "type": "module",
  "scripts": {
    "interop": "node interop.mjs ../../build/mcpdemo && node legacy-interop.mjs ../../build/mcpdemo && node http-interop.mjs ../../build/mcpdemo"
  },
  "dependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@modelcontextprotocol/sdk": "1.30.0"
  }
}
